# 游戏文案图鉴预览指南

本项目是一个纯前端的静态站点。要查看页面效果，只需在项目根目录运行任一静态服务器，然后在浏览器打开对应地址：

```bash
# 启动本地静态服务器（示例使用 Python）
python -m http.server 8000

# 在浏览器访问
http://localhost:8000/
```

如果使用 VS Code 等支持“在浏览器中打开”扩展的编辑器，也可以直接右键 `index.html` 选择“在浏览器中打开”，即可看到前端画面。
